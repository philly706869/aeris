[package]
name    = "aeris-proto-json-shard"
version = { workspace = true }
edition = { workspace = true }
publish = { workspace = true }

[features]
preprocess = ["aeris/ui-preprocess"]

[dependencies]
aeris = { workspace = true }
